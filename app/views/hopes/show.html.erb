<p id="notice"><%= notice %></p>

<p>
  <strong>Content:</strong>
  <%= @hope.content %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag "#{@hope.image}" %>
</p>

<p>
  <strong>User:</strong>
  <%= @hope.user %>
</p>

<div id="target" style="width:475px;height:250px;vertical-align:center;text-align:center;background:#FFFF99;">
  <h2 style="display: inline-block;padding-top:20%;"> <%= @hope.content %></h2>
</div>
<br>
<hr>
<h3>↓↓ここから画像↓↓　（上の対象のDIVを画像化）<h3>
<img src="" id="result" />
<h3>↑↑ここまで画像↑↑</h3>

<hr>

<a href="" id="ss" download="html_ss.png">やりたいこと画像ダウンロード</a>

<hr>
</div>


<script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/0.4.1/html2canvas.js"></script>
<script>
  //ロードされた際の処理として実施：
  window.onload = function(){

    //HTML内に画像を表示
    html2canvas(document.getElementById("target"),{
      onrendered: function(canvas){
        //imgタグのsrcの中に、html2canvasがレンダリングした画像を指定する。
        var imgData = canvas.toDataURL();
        document.getElementById("result").src = imgData;
      }
    });

    //ボタンを押下した際にダウンロードする画像を作る
    html2canvas(document.getElementById("target"),{
      onrendered: function(canvas){
        //aタグのhrefにキャプチャ画像のURLを設定
        var imgData = canvas.toDataURL();
        document.getElementById("ss").href = imgData;
      }
    });

  }
</script>

<%= link_to 'Edit', edit_hope_path(@hope) %> |
<%= link_to 'Back', hopes_path %>